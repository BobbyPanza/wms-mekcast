@page "/not-found"
@inject NavigationManager Nav

<PageTitle>Pagina non trovata</PageTitle>

<div class="d-flex flex-column align-center mt-8 gap-3">
    <MudIcon Icon="@Icons.Material.Filled.SearchOff" Style="font-size:72px;color:#94a3b8;" />
    <MudText Typo="Typo.h6">Pagina non trovata</MudText>
    <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick='() => Nav.NavigateTo("/home")'>
        Torna al menu
    </MudButton>
</div>
